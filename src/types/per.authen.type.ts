export interface PerAuth {
  per_id?: number;
  per_name: string;
  access_diagnose: boolean;
  access_report: boolean;
  access_manageuser: boolean;
  access_position: boolean;
  access_patient: boolean;
}
